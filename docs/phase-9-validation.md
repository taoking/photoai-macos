# Phase 9 验收记录 — Cleanup

日期：2026-08-12  
开发环境：Xcode 27.0 Beta（macOS 27.0 SDK）

## 已完成

- 清理分析在后台、本机执行：同尺寸文件先做 SHA-256 精确重复检查；普通图片使用低分辨率视觉指纹发现相似项。
- 提供 RAW/JPEG 同名配对、截图命名规则与 `-Edited` / `-Web` 等导出文件名关联；所有结果均为建议，不会自动删除。
- 用户可选择建议中的候选文件；“移到废纸篓”需在系统对话框中再次明确确认。成功后才删除对应的本地 Catalog 记录。
- 不可访问来源和缺失文件会逐项保留失败原因，包含外置磁盘断开的可读提示。

## 自动化验证

| 命令 | 结果 |
| --- | --- |
| `DEVELOPER_DIR=/path/to/Xcode-beta.app/Contents/Developer swift test --filter CleanupWorkflowTests` | 通过，3 tests / 1 suite；覆盖五类建议、原始测试文件不变、来源断开失败，以及真实废纸篓调用。 |

真实废纸篓测试只移动测试临时文件，随后立即恢复；没有移动用户文件。

## 本机界面验证

- 已在调试 `.app` 打开“清理”工具页，确认初始状态只显示“分析本地图库”，并明确说明不会自动删除。
- 未启动对用户图库的清理分析，未选择任何候选项，也未执行移到废纸篓。
